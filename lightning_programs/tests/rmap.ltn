var assert_counter = require("assert_counter.ltn")
var assert = assert_counter.assert
var assert_not = assert_counter.assert_not
{
    // Testing rmap
    var n = 100
    var table1 = []
    table.list_init(table1, n)
    function init_in_range (start, end, this_table)
    {
        for (var i = start; i<end; i++)
        {
            this_table[i] = i
        }
    }
    rmap(6, table1, init_in_range)
    for (var i = 0; i<n; i++)
    {
        assert(table1[i], i, "Failed rmap Test 1, iteration: " .. i .. ".")
    }

    rmap(1, table1, init_in_range)
    for (var i = 0; i<n; i++)
    {
        assert(table1[i], i, "Failed rmap Test 2, iteration: " .. i .. ".")
    }

    rmap(3, table1, init_in_range)
    for (var i = 0; i<n; i++)
    {
        assert(table1[i], i, "Failed rmap Test 3, iteration: " .. i .. ".")
    }

    rmap(200, table1, init_in_range)
    for (var i = 0; i<n; i++)
    {
        assert(table1[i], i, "Failed rmap Test 4, iteration: " .. i .. ".")
    }
}
return assert_counter.get_error_counter()